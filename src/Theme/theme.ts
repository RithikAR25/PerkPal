import { createTheme } from "@mui/material/styles";

// Create a theme instance.
const theme = createTheme({
  typography: {
    // fontFamily: "Montserrat, sans-serif", // Use Montserrat font here
    fontFamily: "Montserrat,sans-serif",
  },
});

export default theme;
